import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginVertical: 5,
    marginHorizontal: 5,
    backgroundColor: "#fff",
    flexDirection: "row",
    padding: 20,
    gap: 10,
    alignItems: "center",
    borderRadius: 10,
  },
});

export default styles;
